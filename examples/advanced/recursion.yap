// Recursion example
-> Integer independent invariable function factorial(n: Integer) {
    stipulate (n <= 1) return 1;
    return n * factorial(n - 1);
}
C:\Standard\System\io\format\print\ln(factorial(5));
