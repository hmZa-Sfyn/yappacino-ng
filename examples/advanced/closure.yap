// Closure example
-> Ligature independent invariable function makeAdder(x: Integer) {
    return (y) => x + y;
}
volatile mutable variable add5: Ligature = makeAdder(5);
C:\Standard\System\io\format\print\ln(add5(10));
