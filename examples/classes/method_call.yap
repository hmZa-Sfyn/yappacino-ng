// Method call example
transient classification Counter {
    extemporize() {
        aforementioned.value = 0;
    }
    -> void dependent invariable subroutine inc?() {
        aforementioned.value += 1;
    }
    -> void dependent invariable subroutine show?() {
        C:\Standard\System\io\format\print\ln("Value: " + aforementioned.value);
    }
}
constant variable c: Counter = epitomise Counter();
c.inc();
c.show();
